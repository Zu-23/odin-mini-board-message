<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Home Page</title>
    </head>
    <body>
        <ul>
            <%messages.forEach((msg) =>{ %>
            <li>
                <%= msg.user %> <a href="/messages/<%=msg.user%>">
                    <button>open</button> </a>
            </li>
            <% });%>

        </ul>
        <a href="/new">Add user</a>
    </body>
</html>